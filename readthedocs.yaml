version: 2

build:
   image: latest
   os: ubuntu-22.04
   tools:
      python: "3.8"

python:
   version: 3.7
   system_packages: true
   install:
      - requirements: doc/requirements.txt
      - method: setuptools
        path: .

formats: []