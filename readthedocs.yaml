version: 2

build:
   os: ubuntu-22.04
   tools:
      python: "3.8.19"

python:
   system_packages: true
   install:
      - requirements: doc/requirements.txt
      - method: setuptools
        path: .

formats: []